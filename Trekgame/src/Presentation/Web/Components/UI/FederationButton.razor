@namespace StarTrekGame.Web.Components.UI

<button class="federation-btn @VariantClass @SizeClass @(GlowPulse ? "glow-pulse" : "") @Class"
        style="@Style"
        disabled="@Disabled"
        @onclick="OnClick">
    @if (!string.IsNullOrEmpty(Icon))
    {
        <span class="federation-btn__icon">@Icon</span>
    }
    @if (!string.IsNullOrEmpty(Text))
    {
        <span class="federation-btn__text">@Text</span>
    }
    @ChildContent
</button>

@code {
    [Parameter] public string? Text { get; set; }
    [Parameter] public string? Icon { get; set; }
    [Parameter] public string Variant { get; set; } = "primary"; // primary, secondary, danger, success, outline, pill
    [Parameter] public string Size { get; set; } = "medium"; // small, medium, large
    [Parameter] public bool GlowPulse { get; set; } = false;
    [Parameter] public bool Disabled { get; set; } = false;
    [Parameter] public string? Style { get; set; }
    [Parameter] public string? Class { get; set; }
    [Parameter] public EventCallback<MouseEventArgs> OnClick { get; set; }
    [Parameter] public RenderFragment? ChildContent { get; set; }

    private string VariantClass => Variant switch
    {
        "secondary" => "federation-btn--secondary",
        "danger" => "federation-btn--danger",
        "success" => "federation-btn--success",
        "outline" => "federation-btn--outline",
        "pill" => "federation-btn--pill",
        _ => ""
    };

    private string SizeClass => Size switch
    {
        "small" => "federation-btn--sm",
        "large" => "federation-btn--lg",
        _ => ""
    };
}

<style>
    .federation-btn__icon {
        margin-right: 6px;
    }

    .federation-btn--sm {
        padding: 4px 12px;
        font-size: 10px;
    }

    .federation-btn--lg {
        padding: 12px 28px;
        font-size: 14px;
    }

    .federation-btn--secondary {
        background: var(--ui-secondary);
    }

    .federation-btn:disabled {
        opacity: 0.5;
        cursor: not-allowed;
        filter: grayscale(50%);
    }
</style>
